
import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette:{
    primary:{ main:'#14a10d' },
    secondary:{ main:'#71d400' },
    background:{ default:'#b4ea8b' }
  },
  shape:{ borderRadius:12 }
})

export default theme
